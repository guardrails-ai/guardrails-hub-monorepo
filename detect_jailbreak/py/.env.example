# Environment variables for guardrails-ai-detect-jailbreak
# Copy to .env and fill in real values before running the tests.

# Hugging Face token — required only for gated/private models. (optional)
HF_TOKEN=hf_...

# Optional model source override (defaults to 's3'). (optional)
MODEL_PATH=s3
