# Environment variables for guardrails-ai-detect-prompt-injection
# Copy to .env and fill in real values before running the tests.

# OpenAI API key — used to embed text for the local FAISS similarity check. (required)
OPENAI_API_KEY=sk-...
